这是一份为你专门定制的逻辑规则提示词（Prompt），你可以直接将其发送给你的 Web 开发者工具（如 Cursor、Claude 等），它专注于后端算法逻辑、数据处理流程以及交互状态的管理：

---

### **Prompt: 智能排班 Web 应用逻辑需求文档**

**角色定位：** 你是一位精通排班算法与复杂状态管理的逻辑架构师。请根据以下规则，为我设计一个纯前端运行的智能排班 Web 应用逻辑。

#### **1. 数据解析与预处理层**

* **名单读取：** 应用需解析用户上传的“无课表”CSV/Excel 文件，提取所有学生姓名并去重。
* **手动配置：** 在生成前，网页需列出名单列表，提供交互界面供用户完成两件事：
1. **性别标注：** 标注每位同学为“男”或“女”。
2. **筛选范围：** 勾选哪些同学参与本次排班计算。


* **稀缺度分析（核心算法）：** 在排班前，系统必须统计每位参与同学在所有班次中的“空闲次数总和”。
* **原则：** 优先安排空闲时段最少的同学。例如，A 同学全周只有周一有空，而 B 同学全周都有空，算法必须优先将 A 填入周一，以防 B 占用周一后导致 A 无班可值。



#### **2. 排班核心规则与约束**

* **时段定义：** 周一至周五，每天两班。第一班：14:00-15:20；第二班：15:20-16:50。
* **性别约束：**
* 排班表分为“男生班”和“女生班”名额（每班次各需 1 人）。
* **硬约束：** 女生严禁修男生班。
* **软约束：** 男生优先修男生班。仅当女生人手不足以覆盖女生班时，男生才补入女生班。系统需遵循“男生修女生班人数最小化”原则。


* **单双周调度（灵活分配）：**
* **默认状态：** 一人一周一班。
* **缺口处理：** 若某时段人手不足以覆盖每周，支持将该时段拆分为“单周”和“双周”。例如：时段 1 由 A 同学每周值班，时段 2 由 B 同学单周、C 同学双周交替值班。


* **空白预留：** 若某一班次在满足性别约束的前提下无人可用，则该位置保持为空，不进行强制填充。

#### **3. 公平性与负载均衡**

* **班数均摊：** 在满足稀缺度优先的前提下，算法需计算“目标人均班数”，尽量保证每位同学的总值班频率一致。
* **时段多样性：** 尽量避免同一同学在同一天连续值两班（除非人手极度短缺）。

#### **4. 交互逻辑与状态维护**

* **人员池组件：** 页面顶部设有人员池，显示所有参与同学。
* **状态反馈：** 每个名字标签上需实时显示该同学的“排班总数”及“具体时段预览”。


* **画布交互（拖拽）：**
* 用户可以从人员池拖动名字到表格中进行手动替换。
* 手动修改后，系统需实时重算受影响同学的统计数据（班数、冲突提醒）。
* **冲突检查：** 若用户手动将女生拖入“男生班”，系统需发出视觉警告。



#### **5. 技术执行环境**

* **纯前端逻辑：** 所有解析、统计、排序和排班逻辑必须在浏览器端执行，禁止将用户数据上传至服务器，以实现零服务器资源占用。

---

### **给你的额外建议（针对 Vibecoding）：**

1. **数据结构：** 建议让 AI 使用 `JSON` 格式来存储解析后的数据，例如 `[ {name: "张三", gender: "M", free_slots: ["Mon-1", "Wed-2"], current_assigned: 0} ]`。
2. **算法选择：** 由于你的数据量级较小（几十个人），这个逻辑本质上是一个 **Constraint Satisfaction Problem (约束满足问题)**。你可以要求 AI 使用简单的 **贪心算法 + 优先级权重** 来实现，这在前端执行速度极快。
3. **单双周处理：** 在 UI 上，你可以要求表格的每个格子都有一个“分割”按钮，点击后将该格子一分为二，分别对应单双周。这样逻辑上最清晰。