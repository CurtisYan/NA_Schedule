这是一份为你专门定制的逻辑规则提示词（Prompt），你可以直接将其发送给你的 Web 开发者工具（如 Cursor、Claude 等），它专注于后端算法逻辑、数据处理流程以及交互状态的管理：

---

### **Prompt: 智能排班 Web 应用逻辑需求文档**

**角色定位：** 你是一位精通排班算法与复杂状态管理的逻辑架构师。请根据以下规则，为我设计一个纯前端运行的智能排班 Web 应用逻辑。

#### **1. 数据解析与预处理层**

* **名单读取：** 应用需解析用户上传的“无课表”CSV/Excel 文件，提取所有学生姓名并去重。
* **特定周数解析：** 能够识别无课表 Excel 中的名字后缀信息（例如带括号标注出的 `(1, 18周)` 或者 `(1-18周)` 等约束），并正确提取储存某同学能够值班的具体周数。若无括号标注，则默认该同学 1-18周 全时间有空。
* **手动配置：** 在生成前，网页需列出名单列表，提供交互界面供用户完成两件事：
1. **性别标注：** 标注每位同学为“男”或“女”。
2. **筛选范围：** 勾选哪些同学参与本次排班计算。


#### **2. 全局参数设置与视图管理**

* **过滤与定制排班范围：** 在设置页面，系统必须支持勾选具体的**值班日期**（默认周一至周五）与**时间段**（默认7-8节、9-10节）。不仅是后端逻辑，前端排班表及导出也应该根据选中的设置动态渲染。
* **时段人数配置（Min/Max）：** 系统需要在设置提供每个格子每个性别**单次排班的“最少容量”与“最大容量”**（如最少1人，最多2人）。如果设置最多 2 男 2 女，则单个格子最多存放 4 人。支持该容积的动态拖拽替换与界面可视化。
* **宽广的空闲视图 (Free Time View)：** 提供宽大且美观的弹窗，可以点击同学名字直观查看其每天每节课是否有空，并且要显示该同学所具有的**具体有空的周数明细**。要求内容不要重叠错乱，排版舒适。


#### **3. 排班核心规则与约束（基于两阶段约束满足启发式算法）**

排班算法要采用两阶段动态排班设计（Constraint Satisfaction Heuristic Layer）：

* **阶段一：保本满足最低人数（Min Requirement Pass）**
  1. 优先安排满足 **“最少人数”（Min）**。
  2. **稀缺度优先分配：** 计算所有人在所选周期内的“总空闲次数与周数覆盖率”，空闲时间最少（最难排）的同学拥有最高发班优先权。
  3. **空缺坑位优先填补：** 寻找能选择人数最少（最缺人）的时间点，将其优先分配给最需要班次的同学，防止后面无班可上。

* **阶段二：公平分配多余人数（Extra Capacity Fair Pass）**
  1. 在所有基础位排满后，为了公平，将剩余因为排班极少而未能分配出去的人，填至配置允许的 **“最大人数”（Max）** 空位中。
  2. **班次均摊：** 在填充最大人数空位时，此时**优先判定班次分配状态**：当前被分配班次最少的人拥有最高优先级，强制把大家的排班量（人均班数）一碗水端平。此阶段不应当偏袒时间少的同学，只看当前发班数量。

* **严格时段限制与单双周拆分：**
* 严禁让没有空闲周数的人被排班到对应的格子里（例如A同学明确这节课只有单周有空，则无法放入“周一第三节全周值班”，但可以放入“单周值班”的栏位中）。
* 手动支持点击单个格子进行单双周拆分；拆分后，分别填充，同样遵守上面的容量逻辑和匹配逻辑。
* 支持判断性别冲突（女填男报警）。


#### **4. 交互逻辑与状态维护**

* **人员池组件：** 页面顶部设有人员池，显示所有参与同学。
* **状态反馈：** 每个名字标签上需实时显示该同学的“排班总数”及“冲突/重载等状态预览”。
* **画布交互（拖拽）：**
* 用户可以从人员池拖动名字到表格中进行手动替换。
* 手动修改后，系统需实时重算受影响同学的统计数据（班数、冲突提醒）。
* **导出支持：** 支持将单格内拥挤的多人完整通过 CSV 格式导出一览表。

#### **5. 技术执行环境**

* **纯前端逻辑：** 所有解析、统计、排序和排班逻辑必须在 HTML/JS 浏览器中一站式执行持久化（localStorage），禁止上传至服务器（零服务器负担）。